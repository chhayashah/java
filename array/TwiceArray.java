Given an integer array nums, return true if any value 
appears at least twice in the array, andet  urn false 
if every element is distinct
                   
                 T wiceArra y  { 
                    boolean twic
                (
            for (int  j=  0; j<nums.lenght;j++){
            if(nums[i]==nums[j]){
                   return true;
            }
           }
        }     r

             
    public static  o i d    
    main(

    tring[] args){
        int nums[]={1,2,3,1};
        twice(nums);
    }
}
